<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Devis {{ devis.id }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .button-container {
            margin-top: 20px;
        }
        .button-container a {
            display: inline-block;
            margin-right: 10px;
            padding: 8px 16px;
            text-decoration: none;
            background-color: #4CAF50;
            color: white;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <h1>Devis #{{ devis.id }}</h1>
    <table>
        <tr>
            <th>Garage</th>
            <td>{{ devis.user.firstname }} {{ devis.user.lastname }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ devis.statut }}</td>
        </tr>
        <tr>
            <th>Produits</th>
            <td>
                <ul>
                    {% for produit in devis.produits %}
                        <li>{{ produit.nom }} - {{ produit.prix }} €</li>
                    {% endfor %}
                </ul>
            </td>
        </tr>
        <tr>
            <th>Total Prix</th>
            <td>{{ devis.totalPrix }} €</td>
        </tr>
        <tr>
            <th>Created at</th>
            <td>{{ devis.date ? devis.date|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
    </table>
</body>
</html>
